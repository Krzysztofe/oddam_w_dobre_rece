const OddajFormTop = ({step, text}) => {
    return (
        <>
            <p className='oddajFormTop__p'>{step}</p>
            <h2 className='oddajFormTop__h2'>{text}</h2>

        </>
    );
};

export default OddajFormTop;